
const { WebComponent } = require('./WebComponent');
const { Observer } = require('./ObservedAttirbutes');
const { Attribute } = require('./attribute');
const { Injectable } = require('di-decorator-js');

module.exports = {
  WebComponent, Observer, Attribute, Injectable,
};
